#include "main.h"

/**
 * _islower - Entry Point
 * Description: Check lowercase c
 * Return: 1 if c is lowercase 
* Return: Always 0 (Success)
 */

int _islower(int c)
{
return (c >= 97 && c <= 122);
}
